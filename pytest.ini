[pytest]
# Only the real test suite. Without this, pytest walks the whole tree and tries
# to import the ad-hoc scripts in scripts/, which fail at collection time —
# some of them open network connections on import.
testpaths = backend/tests
norecursedirs = .venv venv scripts frontend data docs *.egg-info
