[pytest]
# Only the real test suite. Without this, pytest walks the whole tree and tries
# to import the ad-hoc scripts and the untracked duplicate checkout, which fail
# at collection time (some of them open network connections on import).
testpaths = backend/tests
norecursedirs = .venv venv roommate-matching-system scripts frontend data docs *.egg-info
